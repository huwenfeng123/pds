package com.zhihuisystem.pds.utils;

public interface PDSGroup {

    public interface GroupV0 {
    }
    // 验证组1(用于区分一个Bean或者Command对象中存在多组验证集合，进行分别验证)
    public interface GroupV1 {
    }

    // 验证组2(用于区分同一个Bean或者Command对象中存在多组验证集合，进行分别验证)
    public interface GroupV2 {
    }

    // 验证组3(用于区分同一个Bean或者Command对象中存在多组验证集合，进行分别验证)
    public interface GroupV3 {
    }

    // 验证组4(用于区分同一个Bean或者Command对象中存在多组验证集合，进行分别验证)
    public interface GroupV4 {
    }

    // 验证组5(用于区分同一个Bean或者Command对象中存在多组验证集合，进行分别验证)
    public interface GroupV5 {
    }

    // 验证组6(用于区分同一个Bean或者Command对象中存在多组验证集合，进行分别验证)
    public interface GroupV6 {
    }

    // 验证组7(用于区分同一个Bean或者Command对象中存在多组验证集合，进行分别验证)
    public interface GroupV7 {
    }

    // 验证组8(用于区分同一个Bean或者Command对象中存在多组验证集合，进行分别验证)
    public interface GroupV8 {
    }

    // 验证组9(用于区分同一个Bean或者Command对象中存在多组验证集合，进行分别验证)
    public interface GroupV9 {
    }

}
